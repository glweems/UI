import React from 'react';
import styled from 'styled-components';

// const pokedexUrl = require('../images/pokedex.svg');

export const Container = styled.div`
  max-width: 90%;
  margin: 0 auto;
  * {
    max-width: 90%;
    margin: 0 auto;
  }
`;

export default { Container };
